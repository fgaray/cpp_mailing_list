#ifndef _H_CONFIG_
#define _H_CONFIG_

#include <string>

const static std::string list_name = "Hackers USACH";

// Connections
const static std::string smtp_server = "";
const static std::string pop_server = "";


// Pooling

const static unsigned int pooling_interval = 60*5; // 5 min

#endif
